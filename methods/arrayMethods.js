// filter 
// returns array of elements that met given condition 
// doesn't change the original array

// map 
// takes an array and gives new array and can give you unique values in the array 
// doesn't change the original array 

// find 
// returns the fisrt element that returns true from the condition

// forEach 

// does whatever is in the function to each element  in the array  no return value 

// some

 // checks if any of the elements in the array meets the condtional provided and returns true or false if none do.

 // every

 // checks if every element in the array meets the conditional provided and returns true or false if one does not.

 // reduce 

//  takes an element and a property usually the total count, and  you then provide it a starting count 

// const testItems = [
//   {price: 15, quantity: 2},
//   {price: 20, quantity: 1},
//   {price: 5, quantity: 4}
// ]

// testItems.reduce((sum,element) => sum + element.quantity,0)

// includes 

// returns wether or not the array has a value you pass to it either a number or a string 